import React from "react";
import { Route, Routes } from "react-router";

export const App: React.FC = () => (
  <div className="app">
    <Routes>
      <Route path="/" element={} />
      <Route path="*" element={} />
    </Routes>
  </div>
);
